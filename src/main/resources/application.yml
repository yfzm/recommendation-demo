server:
  port: 11111
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mashion
    username: root
    password: sjtuybs@x11404
  data:
    mongodb:
      uri: mongodb://localhost:27017/mashion
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: create